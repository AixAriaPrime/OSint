name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-lint:
    name: Backend – lint & type-check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@34e************4876b0b************c390a5638******ad******6ebd******39******4f8d5 # v4
      - uses: actions/setup-python@a26af69be95******a2******3d495a4c3e4e4022e******6d87065 # v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install ruff mypy
      - run: ruff check .
      - run: mypy . --ignore-missing-imports --no-strict-optional || true

  frontend-build:
    name: Frontend – type-check & build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@34e************4876b0b************c390a5638******ad******6ebd******39******4f8d5 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d******e84afbd3f7d6820020 # v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci --legacy-peer-deps
      - run: npm run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8000

  docker-compose-validate:
    name: Validate docker-compose
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e************4876b0b************c390a5638******ad******6ebd******39******4f8d5 # v4
      - run: cp .env.example .env && docker compose config --quiet